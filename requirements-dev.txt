# Dependências usadas apenas em desenvolvimento.
# Instaladas no target `dev` do Dockerfile e dentro do devcontainer.
# NÃO entram na imagem `prod` que vai para o EKS.

-r requirements.txt

debugpy>=1.8,<2.0      # debugger remoto para "Attach" do VS Code (porta 5678)
ipdb>=0.13,<1.0        # REPL/pdb melhorado para depuração interativa
ruff>=0.5,<1.0         # linter + formatter rápido (substitui flake8 + black)
watchfiles>=0.21,<1.0  # hot-reload mais ágil para o uvicorn --reload
httpx>=0.27,<1.0       # cliente HTTP usado para testar a API manualmente
pytest>=8.2,<9.0       # executor da suite automatizada
pytest-cov>=5.0,<7.0   # relatório de cobertura dos testes
